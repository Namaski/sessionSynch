{% extends 'base.html.twig' %}

{% block title %}Add Category!{% endblock %}

{% block body %}

<div class="wrapper">
    
    <h1>Ajouter une Catégory</h1>

    {{ form_start(form) }}

    <div class="row">
        <div class="col-sm">{{ form_row(form.nameCategory) }}</div>
    </div>

    {{ form_row(form.Valider) }}

    {{ form_end(form) }}

</div>

{% endblock %}